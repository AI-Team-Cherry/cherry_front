export default function Gallery() {
  return (
    <div className="card" style={{ padding: 20 }}>
      <h2>이미지 갤러리</h2>
      <p style={{ color: "var(--color-muted)" }}>
        백엔드의 이미지/생성 API에 맞춰 목록/업로드/변환 UI를 붙이면 됩니다.
      </p>
    </div>
  );
}
